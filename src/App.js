import './App.css';
import Counter from './components/Counter';

function App() {
  return (
    <div>
      <Counter />
    </div>
  );
}
//given a number and counts down until zero
//you enter a number, then it displays a countdown
//when it hits zero, it finishes and prompts you to enter a new number

export default App;
